# BlueMap LZMA2
An addon for BlueMap, adding LZMA2 (XZ) compression support.

## Usage
- download the [bluemap-lzma2.jar](https://github.com/BlueMap-Minecraft/BlueMapLZMA2/releases)
- place the jar file into the `addons` folder next to your bluemap config files
- change the `compression` in your storage-config (`storages/file.conf` or `storages/sql.conf`) to `"bluemap-lzma2:lzma2"` or `"bluemap-lzma2:lzma2-<level>"` where the level is from 0 to 9
- reload bluemap


https://github.com/BlueMap-Minecraft/BlueMapBrotli  but for lzma2 aka xz
gemini 3 falsh promnt: rewrite the whole plugin to do lzma2 aka xz files instead of brotli
